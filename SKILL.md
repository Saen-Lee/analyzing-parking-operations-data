---
name: analyzing-parking-operations-data
description: Use when analyzing parking facility exit records from Excel or CSV to report traffic, vehicle categories, revenue when available, daily trends, and data-quality issues. 适用于停车场出场明细运营分析；不用于仅凭入场或订单数据推定出场车流。
---

# 停车场运营数据分析

依据原始出场明细形成可追溯的经营分析。保留原文件；把字段映射、统计口径及未解决的歧义写进结果。示例见 [sample-data.csv](examples/sample-data.csv) 与 [example-output.md](examples/example-output.md)，验收场景见 [test-cases.md](tests/test-cases.md)。

## 输入与字段确认

1. 读取用户提供的 Excel/CSV，识别工作表、行数、时间范围、记录粒度及候选字段。优先找实际出场时间、出场事件/状态、车辆或订单类型、车牌、实收/有效收费金额；记录采用的字段及原始名称。
2. 可根据列名和样值识别“离场时间”“车辆驶出时间”“出口时间”“出车时间”等同义字段。若多个列都可能代表实际出场，或文件只有入场/订单时间，先询问，不能自行选择或以其他时间替代。
3. 保留原始车辆类型标签。只有在数据字典、字段说明或样值足以确认含义时，才把“临时车、固定车、月卡车、访客车、储值车、免费车”等归并为临停、月租或其他。不能可靠归并时按原类别报告并询问；不要强行二分。
4. 金额分析仅在存在可信的实收/有效收费字段，且确认其与出场记录的对应关系后进行。应收、折扣、退款、订单总额不自动视为实收；多笔支付和多条出场可能造成重复求和时先确认粒度。金额缺失时仍分析车流，并明确收入不可计算；月租出场行的 0 元不代表月租业务收入为 0。

## 统计与异常

- **车流**：1 条有效出场记录 = 1 次车流。同车牌在不同时间多次有效出场分别计数，不按车牌去重。
- **日期**：按实际出场日期归属每日车流，月度为该月每日结果汇总；时间格式或时区不明确且影响日期归属时先询问。
- **有效性**：确认记录是出场事件且出场时间可解析。排除或纳入的判断需有依据并在报告中列明；不要静默删除行。
- **异常**：标记完全相同或高度相似的疑似重复行、关键字段缺失、无效日期、未知类型、负数或异常金额及记录粒度冲突，附行号/标识和处理状态。相同车牌本身不是重复证据。对于会改变结果的疑点，先给待确认事项；必要时展示明确标注的暂计值或上下界，不把它们当作最终指标。
- **原始数据**：不覆盖或修改用户输入。需要清洗时在副本或分析结果中保留原值、处理规则及异常清单。

## 交付

按数据支持的范围提供：数据概况（来源、期间、行数、字段映射）、核心指标（总车流、可确认的分类车流及可计算的收入）、按出场日的趋势、异常与待确认事项、基于数据的运营观察。注明统计分母、币种/单位和收入范围；区分事实、推断与建议，不编造缺失指标。若关键字段无法可靠确定，先提出最少的澄清问题，再完成依赖该字段的统计。
